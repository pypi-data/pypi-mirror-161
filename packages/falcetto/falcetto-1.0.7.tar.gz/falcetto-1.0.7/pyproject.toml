[tool.poetry]
name = "falcetto"
version = "1.0.7"
description = "Just a dummy project."
authors = ["stefanondisponibile"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
