#!/usr/bin/env python3

import os
import sys
from adachi_resource_assistant.utils import findImages, fillImage

if "__main__" == __name__:
    argc = len(sys.argv)
    images = sys.argv[1:] if argc > 1 else findImages(os.getcwd(), extension=".png")

    for image in images:
        fillImage(image)
