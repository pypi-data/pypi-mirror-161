[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'
[project]
name = 'ghopper'
version = '0.0.1'
authors = [{name="João Rocha"}]
description = 'Framework that evaluates strategies to mitigate the phase ordering problem.'
readme = 'README.md'
requires-python = '>=3.7'
[project.scripts]
ghopper = 'ghopper.app:run'
