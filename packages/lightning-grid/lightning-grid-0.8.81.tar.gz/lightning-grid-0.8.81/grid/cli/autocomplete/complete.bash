# DO NOT EDIT. Autogenerated with:
# _GRID_COMPLETE=source_bash grid > grid/autocomplete/complete.bash
# See: https://click.palletsprojects.com/en/7.x/bashcomplete/

_grid_completion() {
    local IFS=$'
'
    COMPREPLY=( $( env COMP_WORDS="${COMP_WORDS[*]}" \
                   COMP_CWORD=$COMP_CWORD \
                   _GRID_COMPLETE=complete $1 ) )
    return 0
}

_grid_completionetup() {
    local COMPLETION_OPTIONS=""
    local BASH_VERSION_ARR=(${BASH_VERSION//./ })
    # Only BASH version 4.4 and later have the nosort option.
    if [ ${BASH_VERSION_ARR[0]} -gt 4 ] || ([ ${BASH_VERSION_ARR[0]} -eq 4 ] && [ ${BASH_VERSION_ARR[1]} -ge 4 ]); then
        COMPLETION_OPTIONS="-o nosort"
    fi

    complete $COMPLETION_OPTIONS -F _grid_completion grid
}

_grid_completionetup;
